//#region src/adapters/interfaces/cron-adapter.interface.d.ts
interface CronLock {
  jobName: string;
  lockId: string;
  acquiredAt: number;
  expiresAt: number;
  instanceId: string;
}
interface CronLockInfo {
  jobName: string;
  instanceId: string;
  acquiredAt: number;
  expiresAt: number;
}
interface CronAdapterConfig {
  lockTTL?: number;
  lockRetryDelay?: number;
  lockRetryAttempts?: number;
  instanceId?: string;
  enableHealthCheck?: boolean;
}
interface CronAdapter {
  acquireLock(jobName: string, ttl: number): Promise<CronLock | null>;
  releaseLock(lock: CronLock): Promise<void>;
  renewLock(lock: CronLock, ttl: number): Promise<boolean>;
  isHealthy(): Promise<boolean>;
  shutdown(): Promise<void>;
  getActiveLocks(): Promise<CronLockInfo[]>;
}
//#endregion
export { CronAdapter, CronAdapterConfig, CronLock, CronLockInfo };
//# sourceMappingURL=cron-adapter.interface.d.mts.map