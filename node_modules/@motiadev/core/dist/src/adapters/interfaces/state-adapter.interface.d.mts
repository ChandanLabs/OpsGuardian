import { InternalStateManager } from "../../types.mjs";

//#region src/adapters/interfaces/state-adapter.interface.d.ts
interface StateItem {
  groupId: string;
  key: string;
  type: 'string' | 'number' | 'boolean' | 'object' | 'array' | 'null';
  value: string | number | boolean | object | unknown[] | null;
}
interface StateFilter {
  valueKey: string;
  operation: 'eq' | 'neq' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'notContains' | 'startsWith' | 'endsWith' | 'isNotNull' | 'isNull';
  value: string;
}
interface StateItemsInput {
  groupId?: string;
  filter?: StateFilter[];
}
interface StateAdapter extends InternalStateManager {
  clear(traceId: string): Promise<void>;
  cleanup(): Promise<void>;
  keys(traceId: string): Promise<string[]>;
  traceIds(): Promise<string[]>;
  items(input: StateItemsInput): Promise<StateItem[]>;
}
//#endregion
export { StateAdapter, StateFilter, StateItem, StateItemsInput };
//# sourceMappingURL=state-adapter.interface.d.mts.map