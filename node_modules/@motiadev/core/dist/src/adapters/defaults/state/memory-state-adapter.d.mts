import { StateAdapter, StateItem, StateItemsInput } from "../../interfaces/state-adapter.interface.mjs";

//#region src/adapters/defaults/state/memory-state-adapter.d.ts
declare class MemoryStateAdapter implements StateAdapter {
  private state;
  constructor();
  getGroup<T>(groupId: string): Promise<T[]>;
  get<T>(traceId: string, key: string): Promise<T | null>;
  set<T>(traceId: string, key: string, value: T): Promise<T>;
  delete<T>(traceId: string, key: string): Promise<T | null>;
  clear(traceId: string): Promise<void>;
  keys(traceId: string): Promise<string[]>;
  traceIds(): Promise<string[]>;
  items(input: StateItemsInput): Promise<StateItem[]>;
  cleanup(): Promise<void>;
  private _makeKey;
}
//#endregion
export { MemoryStateAdapter };
//# sourceMappingURL=memory-state-adapter.d.mts.map