import { CronAdapter, CronLock, CronLockInfo } from "../../interfaces/cron-adapter.interface.mjs";

//#region src/adapters/defaults/cron/in-memory-cron-adapter.d.ts
declare class InMemoryCronAdapter implements CronAdapter {
  private locks;
  private instanceId;
  constructor();
  acquireLock(jobName: string, ttl: number): Promise<CronLock | null>;
  releaseLock(lock: CronLock): Promise<void>;
  renewLock(lock: CronLock, ttl: number): Promise<boolean>;
  isHealthy(): Promise<boolean>;
  shutdown(): Promise<void>;
  getActiveLocks(): Promise<CronLockInfo[]>;
}
//#endregion
export { InMemoryCronAdapter };
//# sourceMappingURL=in-memory-cron-adapter.d.mts.map