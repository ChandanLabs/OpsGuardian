import { StreamAdapter } from "../../interfaces/stream-adapter.interface.mjs";
import { StreamAdapterManager } from "../../interfaces/stream-adapter-manager.interface.mjs";

//#region src/adapters/defaults/stream/file-stream-adapter-manager.d.ts
declare class FileStreamAdapterManager implements StreamAdapterManager {
  private readonly baseDir;
  private readonly motiaFileStoragePath;
  constructor(baseDir: string, motiaFileStoragePath?: string);
  createStream<TData>(streamName: string): StreamAdapter<TData>;
}
//#endregion
export { FileStreamAdapterManager };
//# sourceMappingURL=file-stream-adapter-manager.d.mts.map