import { BaseStreamItem } from "../../../types-stream.mjs";
import { StreamAdapter } from "../../interfaces/stream-adapter.interface.mjs";

//#region src/adapters/defaults/stream/file-stream-adapter.d.ts

declare class FileStreamAdapter<TData> extends StreamAdapter<TData> {
  private readonly filePath;
  private readonly streamsDir;
  constructor(filePath: string, streamName: string, motiaFileStoragePath?: string);
  init(): void;
  getGroup(groupId: string): Promise<BaseStreamItem<TData>[]>;
  get(groupId: string, key: string): Promise<BaseStreamItem<TData> | null>;
  set(groupId: string, id: string, value: TData): Promise<TData & {
    id: string;
  }>;
  delete(groupId: string, id: string): Promise<BaseStreamItem<TData> | null>;
  clear(groupId: string): Promise<void>;
  protected _makeKey(groupId: string, id: string): string;
  protected _readFile(): Record<string, string>;
  protected _writeFile(data: unknown): void;
}
//#endregion
export { FileStreamAdapter };
//# sourceMappingURL=file-stream-adapter.d.mts.map