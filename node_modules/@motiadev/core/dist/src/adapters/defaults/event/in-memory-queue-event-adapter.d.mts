import { EventAdapter, SubscriptionHandle } from "../../interfaces/event-adapter.interface.mjs";
import { Event, QueueConfig } from "../../../types.mjs";
import { QueueMetrics } from "../../../queue-manager.mjs";

//#region src/adapters/defaults/event/in-memory-queue-event-adapter.d.ts
declare class InMemoryQueueEventAdapter implements EventAdapter {
  private queueManager;
  private subscriptions;
  emit<TData>(event: Event<TData>): Promise<void>;
  subscribe<TData>(topic: string, stepName: string, handler: (event: Event<TData>) => void | Promise<void>, queueConfig?: QueueConfig): Promise<SubscriptionHandle>;
  unsubscribe(handle: SubscriptionHandle): Promise<void>;
  shutdown(): Promise<void>;
  getSubscriptionCount(topic: string): Promise<number>;
  listTopics(): Promise<string[]>;
  getAllMetrics(): Record<string, QueueMetrics>;
}
//#endregion
export { InMemoryQueueEventAdapter };
//# sourceMappingURL=in-memory-queue-event-adapter.d.mts.map