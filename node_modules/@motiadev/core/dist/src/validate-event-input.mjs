import { globalLogger } from "./logger.mjs";
import { schemaToJsonSchema } from "./schema-utils.mjs";
import { Printer } from "./printer.mjs";
import Ajv from "ajv";

//#region src/validate-event-input.ts
const ajv = new Ajv({
	allErrors: true,
	strict: false
});
const validateEventInput = (step, event, motia) => {
	const { data } = event;
	const logger = event.logger ? event.logger.child({ step: step.config.name }) : motia.loggerFactory.create({
		step: step.config.name,
		traceId: event.traceId
	});
	if (step.config.input) {
		if (data === null || typeof data !== "object") {
			logger.warn(`⚠️ Event "${step.config.name}" received non-object data`, { data });
			return;
		}
		let compiledSchema;
		const inputSchema = step.config.input;
		try {
			compiledSchema = schemaToJsonSchema(inputSchema);
			if (!compiledSchema) compiledSchema = inputSchema;
		} catch (err) {
			logger.error(`❌ Failed to convert or compile schema for step "${step.config.name}"`, { error: err });
			globalLogger.error("[step handler] failed to convert schema", {
				step: step.config.name,
				error: err
			});
			return;
		}
		let validate;
		try {
			validate = ajv.compile(compiledSchema);
		} catch (err) {
			logger.error(`❌ Invalid JSON schema for step "${step.config.name}"`, { error: err });
			globalLogger.error("[step handler] invalid JSON schema", {
				step: step.config.name,
				error: err
			});
			return;
		}
		if (!validate(data) && validate.errors?.length) {
			const missingFields = [];
			const extraFields = [];
			const typeMismatches = [];
			for (const err of validate.errors) {
				const field = err.instancePath ? err.instancePath.replace(/^\//, "").replace(/\//g, ".") : "(root)";
				switch (err.keyword) {
					case "required":
						if ("missingProperty" in err.params) missingFields.push(field === "(root)" ? err.params.missingProperty : `${field}.${err.params.missingProperty}`);
						break;
					case "type":
						typeMismatches.push(`Field "${field}": must be ${err.params.type}`);
						break;
					case "additionalProperties":
						if ("additionalProperty" in err.params) extraFields.push(field === "(root)" ? err.params.additionalProperty : `${field}.${err.params.additionalProperty}`);
						break;
					default: typeMismatches.push(`Field "${field}": ${err.message}`);
				}
			}
			(motia?.printer ?? new Printer(process.cwd())).printEventInputValidationError({ topic: event.topic }, {
				missingFields,
				extraFields,
				typeMismatches
			});
		}
	}
};

//#endregion
export { validateEventInput };
//# sourceMappingURL=validate-event-input.mjs.map