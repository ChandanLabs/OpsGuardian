import { generateTypeFromSchema } from "./generate-type-from-schema.mjs";

//#region src/types/generate-types-from-response.ts
const generateTypesFromResponse = (record) => {
	return Object.entries(record).map(([status, schema]) => {
		return `ApiResponse<${status}, ${generateTypeFromSchema(schema)}>`;
	}).join(" | ");
};

//#endregion
export { generateTypesFromResponse };
//# sourceMappingURL=generate-types-from-response.mjs.map