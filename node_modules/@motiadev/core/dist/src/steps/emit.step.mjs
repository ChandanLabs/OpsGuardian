import { z } from "zod";

//#region src/steps/emit.step.ts
const config = {
	type: "api",
	name: "Event Emitter",
	description: "System endpoint for emitting events",
	path: "/emit",
	method: "POST",
	emits: [],
	flows: ["_system"],
	bodySchema: z.object({
		topic: z.string(),
		data: z.record(z.string(), z.any())
	})
};

//#endregion
export { config };
//# sourceMappingURL=emit.step.mjs.map