import * as zustand0 from "zustand";
import * as zustand_middleware0 from "zustand/middleware";

//#region src/stores/use-theme-store.d.ts
type Theme = 'dark' | 'light' | 'system';
type Actions = {
  setTheme: (theme: Theme) => void;
};
type State = {
  theme: Theme;
};
type ThemeState = State & Actions;
declare const useThemeStore: zustand0.UseBoundStore<Omit<zustand0.StoreApi<ThemeState>, "setState" | "persist"> & {
  setState(partial: ThemeState | Partial<ThemeState> | ((state: ThemeState) => ThemeState | Partial<ThemeState>), replace?: false | undefined): unknown;
  setState(state: ThemeState | ((state: ThemeState) => ThemeState), replace: true): unknown;
  persist: {
    setOptions: (options: Partial<zustand_middleware0.PersistOptions<ThemeState, unknown, unknown>>) => void;
    clearStorage: () => void;
    rehydrate: () => Promise<void> | void;
    hasHydrated: () => boolean;
    onHydrate: (fn: (state: ThemeState) => void) => () => void;
    onFinishHydration: (fn: (state: ThemeState) => void) => () => void;
    getOptions: () => Partial<zustand_middleware0.PersistOptions<ThemeState, unknown, unknown>>;
  };
}>;
//#endregion
export { Theme, ThemeState, useThemeStore };
//# sourceMappingURL=use-theme-store.d.ts.map