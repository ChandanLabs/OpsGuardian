import { JsonSchema, LockedData, Step, StreamAdapterManager, StreamAuthConfig } from "@motiadev/core";
import { RedisClientType } from "redis";

//#region src/generate-locked-data.d.ts
declare const collectFlows: (projectDir: string, lockedData: LockedData) => Promise<Step[]>;
type StreamAuthOptions = {
  authenticate: StreamAuthConfig['authenticate'];
  contextSchema?: JsonSchema;
};
declare const generateLockedData: (config: {
  projectDir: string;
  streamAdapter?: StreamAdapterManager;
  redisClient?: RedisClientType;
  printerType?: "disabled" | "default";
  streamAuth?: StreamAuthOptions;
}) => Promise<LockedData>;
//#endregion
export { collectFlows, generateLockedData };
//# sourceMappingURL=generate-locked-data.d.mts.map