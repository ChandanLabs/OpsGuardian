import fs from "node:fs";
import path from "node:path";

//#region src/constants.ts
function getWorkbenchBase() {
	const basePath = process.env.MOTIA_WORKBENCH_BASE;
	if (basePath === "/") return "";
	return basePath && basePath[0] !== "/" ? `/${basePath}` : basePath || "";
}
const workbenchBase = getWorkbenchBase();
const isTutorialDisabled = process.env.MOTIA_TUTORIAL_DISABLED === "true" || !fs.existsSync(path.join(process.cwd(), "tutorial/tutorial.tsx"));

//#endregion
export { isTutorialDisabled, workbenchBase };
//# sourceMappingURL=constants.mjs.map