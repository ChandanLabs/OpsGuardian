//#region src/cloud/new-deployment/utils/pretty-bytes.ts
const gigabyte = 1024 * 1024 * 1024;
const megabyte = 1024 * 1024;
const kilobyte = 1024;
const prettyBytes = (size) => {
	if (size > gigabyte) return `${Math.round(size / gigabyte)}gb`;
	if (size > megabyte) return `${Math.round(size / megabyte)}mb`;
	if (size > kilobyte) return `${Math.round(size / kilobyte)}kb`;
	return `${size}b`;
};

//#endregion
export { prettyBytes };
//# sourceMappingURL=pretty-bytes.mjs.map