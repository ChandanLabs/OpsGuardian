import path from "path";
import pc from "picocolors";

//#region src/cloud/build/validations/duplicate-step-names.validator.ts
const duplicateStepNamesValidator = (builder) => {
	const errors = [];
	const stepNames = /* @__PURE__ */ new Set();
	for (const step of Object.values(builder.stepsConfig)) if (stepNames.has(step.config.name)) errors.push({
		relativePath: path.relative(builder.projectDir, step.filePath),
		message: [`Duplicate step names: ${pc.red(step.config.name)}`].join("\n"),
		step
	});
	else stepNames.add(step.config.name);
	return {
		errors,
		warnings: []
	};
};

//#endregion
export { duplicateStepNamesValidator };
//# sourceMappingURL=duplicate-step-names.validator.mjs.map